# Tasks

- [x] 전체 UI 구조 분석을 통한 곡률 조정 (`rounded-full`, `rounded-[2rem]`, `rounded-3xl` 클래스 검색)
- [x] 전반적인 Card, 컨테이너 등의 곡률을 `rounded-lg` 혹은 `rounded-xl` 수준으로 낮추어 전문성 강화
- [x] `page.tsx` 내 `<TabsList>` 및 `<TabsTrigger>` 디자인을 하단 보더 하이라이팅 방식 혹은 각진 모서리로 대폭 수정
- [x] `AnalysisForm.tsx` 내 검색창 형태의 폼 UI를 직선적이고 정갈한 디자인으로 교체
- [x] `AgentReportCard.tsx` 내 `DeepDivePane`의 `메인 팔레트` 열 태그(`bg-amber-50 text-amber-700`) 잘림 문제 픽스 (줄바꿈 설정 및 min-width 적용)
- [x] 최종 빌드 및 화면 비율, 텍스트 가독성 검증
